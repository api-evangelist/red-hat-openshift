naftiko: 1.0.0-alpha2
info:
  label: Red Hat OpenShift Cluster Manager API — Identity Providers
  description: 'Red Hat OpenShift Cluster Manager API — Identity Providers. 2 operations. Lead operation: List Identity Providers.
    Self-contained Naftiko capability covering one Red Hat Openshift business surface.'
  tags:
  - Red Hat Openshift
  - Identity Providers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    RED_HAT_OPENSHIFT_API_KEY: RED_HAT_OPENSHIFT_API_KEY
capability:
  consumes:
  - type: http
    namespace: cluster-manager-identity-providers
    baseUri: https://api.openshift.com
    description: Red Hat OpenShift Cluster Manager API — Identity Providers business capability. Self-contained, no shared
      references.
    resources:
    - name: api-clusters_mgmt-v1-clusters-cluster_id-identity_providers
      path: /api/clusters_mgmt/v1/clusters/{cluster_id}/identity_providers
      operations:
      - name: listidentityproviders
        method: GET
        description: List Identity Providers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createidentityprovider
        method: POST
        description: Create Identity Provider
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.RED_HAT_OPENSHIFT_API_KEY}}'
  exposes:
  - type: rest
    namespace: cluster-manager-identity-providers-rest
    port: 8080
    description: REST adapter for Red Hat OpenShift Cluster Manager API — Identity Providers. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/clusters-mgmt/v1/clusters/{cluster-id}/identity-providers
      name: api-clusters-mgmt-v1-clusters-cluster-id-identity-providers
      description: REST surface for api-clusters_mgmt-v1-clusters-cluster_id-identity_providers.
      operations:
      - method: GET
        name: listidentityproviders
        description: List Identity Providers
        call: cluster-manager-identity-providers.listidentityproviders
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createidentityprovider
        description: Create Identity Provider
        call: cluster-manager-identity-providers.createidentityprovider
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cluster-manager-identity-providers-mcp
    port: 9090
    transport: http
    description: MCP adapter for Red Hat OpenShift Cluster Manager API — Identity Providers. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: list-identity-providers
      description: List Identity Providers
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cluster-manager-identity-providers.listidentityproviders
      outputParameters:
      - type: object
        mapping: $.
    - name: create-identity-provider
      description: Create Identity Provider
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: cluster-manager-identity-providers.createidentityprovider
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
