naftiko: 1.0.0-alpha2
info:
  label: Red Hat OpenShift Container Platform API — Builds
  description: 'Red Hat OpenShift Container Platform API — Builds. 3 operations. Lead operation: List Build Configs. Self-contained
    Naftiko capability covering one Red Hat Openshift business surface.'
  tags:
  - Red Hat Openshift
  - Builds
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    RED_HAT_OPENSHIFT_API_KEY: RED_HAT_OPENSHIFT_API_KEY
capability:
  consumes:
  - type: http
    namespace: red-hat-openshift-builds
    baseUri: https://api.{cluster}:6443
    description: Red Hat OpenShift Container Platform API — Builds business capability. Self-contained, no shared references.
    resources:
    - name: apis-build.openshift.io-v1-namespaces-namespace-buildconfigs
      path: /apis/build.openshift.io/v1/namespaces/{namespace}/buildconfigs
      operations:
      - name: listbuildconfigs
        method: GET
        description: List Build Configs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createbuildconfig
        method: POST
        description: Create Build Config
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: apis-build.openshift.io-v1-namespaces-namespace-builds
      path: /apis/build.openshift.io/v1/namespaces/{namespace}/builds
      operations:
      - name: listbuilds
        method: GET
        description: List Builds
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.RED_HAT_OPENSHIFT_API_KEY}}'
  exposes:
  - type: rest
    namespace: red-hat-openshift-builds-rest
    port: 8080
    description: REST adapter for Red Hat OpenShift Container Platform API — Builds. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/apis/build-openshift-io/v1/namespaces/{namespace}/buildconfigs
      name: apis-build-openshift-io-v1-namespaces-namespace-buildconfigs
      description: REST surface for apis-build.openshift.io-v1-namespaces-namespace-buildconfigs.
      operations:
      - method: GET
        name: listbuildconfigs
        description: List Build Configs
        call: red-hat-openshift-builds.listbuildconfigs
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createbuildconfig
        description: Create Build Config
        call: red-hat-openshift-builds.createbuildconfig
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/apis/build-openshift-io/v1/namespaces/{namespace}/builds
      name: apis-build-openshift-io-v1-namespaces-namespace-builds
      description: REST surface for apis-build.openshift.io-v1-namespaces-namespace-builds.
      operations:
      - method: GET
        name: listbuilds
        description: List Builds
        call: red-hat-openshift-builds.listbuilds
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: red-hat-openshift-builds-mcp
    port: 9090
    transport: http
    description: MCP adapter for Red Hat OpenShift Container Platform API — Builds. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: list-build-configs
      description: List Build Configs
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: red-hat-openshift-builds.listbuildconfigs
      outputParameters:
      - type: object
        mapping: $.
    - name: create-build-config
      description: Create Build Config
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: red-hat-openshift-builds.createbuildconfig
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: list-builds
      description: List Builds
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: red-hat-openshift-builds.listbuilds
      outputParameters:
      - type: object
        mapping: $.
